---
interface Benefit {
  title: string;
  description: string;
  icon: string;
}

interface Props {
  title: string;
  benefits: Benefit[];
}

const { title, benefits } = Astro.props;
---

<section data-testid="lp-benefits" class="py-16 md:py-24 bg-surface">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl md:text-4xl font-bold text-text-primary text-center mb-4 animate-on-scroll">
      {title}
    </h2>
    <div class="w-16 h-1 bg-accent-500 mx-auto rounded mb-12"></div>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
      {benefits.map((benefit, i) => (
        <div
          class="animate-on-scroll text-center p-8"
          style={`animation-delay: ${i * 100}ms`}
        >
          <div class="w-20 h-20 bg-primary-50 rounded-2xl flex items-center justify-center mx-auto mb-5" aria-hidden="true">
            {benefit.icon.startsWith("M") ? (
              <svg class="w-10 h-10 text-primary-700" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" d={benefit.icon} />
              </svg>
            ) : (
              <span class="text-4xl">{benefit.icon}</span>
            )}
          </div>
          <h3 class="text-xl font-bold text-text-primary mb-3">{benefit.title}</h3>
          <p class="text-text-body leading-relaxed">{benefit.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
